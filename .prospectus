Prospectus.extra_dep('file', 'pkgforge')
Prospectus.extra_dep('file', 'prospectus_pkgforge')

item do
  expected do
    url_xpath
    url 'http://sourceware.org/git/?p=lvm2.git;a=tags'
    xpath '/html/body/table/tr[1]/td[2]/a'
    regex /^v(\d+)_(\d+)_(\d+)$/, '\1.\2.\3'
  end

  actual do
    git_tag
    regex /^(.*)-\d+$/
  end

  extend ProspectusPkgforge::DepItems
end
